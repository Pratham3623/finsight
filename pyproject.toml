[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "finsight"
version = "0.1.0"
description = "Financial Data Engineering & Analytics Platform"
requires-python = ">=3.11,<3.13"

[tool.setuptools.packages.find]
where = ["src"]
